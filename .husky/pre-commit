#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# BMAD Status Auto-Sync
echo "🔄 Running BMAD status sync..."
npm run bmad:sync

# Stage the updated status file
git add docs/BMAD_STATUS.md

# Also stage any updated story frontmatter (last_sync timestamps)
git add docs/stories/*.md

echo "✅ BMAD sync complete"
